// const orginalUserController = require('strapi-plugin-users-permissions/controllers/Auth');


module.exports = {

    // async register(ctx) {
    //     const result = await orginalUserController.register(ctx)
    //     console.debug('register', result)
    //     return result
    // },

    // async callback(ctx) {
    //     const result = await orginalUserController.callback(ctx)
    //     console.debug('callback', result)
    //     return result
    // }
};
